import { styled } from '../../../stitches.config'

export const PatientsContainer = styled('div', {
  display: 'flex',
  width: '100%',
  padding: 20,
  height: '100%',
  gap: '$4',
  marginRight: '$2',
})
