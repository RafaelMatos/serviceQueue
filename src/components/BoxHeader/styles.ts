import { styled } from '../../../stitches.config'

export const Container = styled('div', {
  borderBottom: '1px solid $gray700',
  padding: '0 $2 $4',
  width: '100%',
  marginBottom: '$4',
})
